---
"taiwan-exchange-rates": patch
---

Keep web results consistent with their requested quote type and ignore superseded responses and errors. Preserve snapshot recording timestamps and order in CLI history tables without changing JSON or CSV output schemas.
